# Postgres Migration Audit

> Goal: Make Fikiri's backend functionally and idiomatically Postgres-native across every feature, with no SQLite-isms left in runtime paths. No workarounds.

## Decisions (locked 2026-05-10)

| # | Decision | Detail |
|---|---|---|
| 1 | **Postgres-only runtime** | Drop SQLite code branches and SQLite-specific SQL from runtime modules. Tests and local dev move to a real Postgres (Docker / managed dev DB). |
| 2 | **Schema-as-is by default** | Do not change column types, constraints, defaults, or indexes without explicit approval. Behavioral fixes that don't touch schema can ship freely. Any column-shape change is paused for review. |
| 3 | **Branch + per-feature PRs** | Audit branch: `chore/pg-audit`. One PR per feature area (CRM, Email, Billing, Auth, Webhooks, Privacy, Automations, Analytics, Onboarding, Idempotency/RateLimit/Logging, Misc). |

## Scope

Runtime backend Python modules:

```
analytics/   core/   crm/   email_automation/   integrations/   routes/   services/
app.py       scripts/prestart.py
```

Out of scope (for now):

- Test files (`tests/`) — covered by Phase 3.
- One-off migration scripts (`scripts/migrations/`).
- Legacy stubs flagged `# deprecated` (frozen, no new imports).

## What this audit catches

Beyond the existing `scripts/check_postgres_sql_patterns.py` (which only flags 5 narrow patterns), this audit treats the following classes as bugs:

| Class | Pattern | Postgres-correct replacement |
|---|---|---|
| Upsert | `INSERT OR REPLACE`, `INSERT OR IGNORE` | `INSERT ... ON CONFLICT (...) DO UPDATE/NOTHING` |
| Auto-id | `INTEGER PRIMARY KEY AUTOINCREMENT` | `BIGSERIAL PRIMARY KEY` (or `GENERATED BY DEFAULT AS IDENTITY`) — **schema change, ask first** |
| Insert-id | `cursor.lastrowid`, `last_insert_rowid()` | `INSERT ... RETURNING id` |
| Booleans | `WHERE bool_col = 1/0` | `WHERE bool_col = TRUE/FALSE` (and stop storing booleans in `INTEGER` columns) |
| Datetimes from DB | `datetime.fromisoformat(row['col'])` | accept native `datetime` from psycopg2 |
| Datetime TEXT comparisons | `row['ts'] > timestamptz` | cast TEXT columns explicitly, or change schema to TIMESTAMP — **ask first** |
| SQLite date funcs | `datetime('now', ...)`, `julianday()`, `strftime()`, `date('now')` | `NOW()` / `CURRENT_TIMESTAMP` / `INTERVAL` arithmetic / `to_char()` / `EXTRACT()` |
| Null-handling | `IFNULL(a, b)` | `COALESCE(a, b)` |
| Concat | `'a' \|\| 'b'` | both work, but prefer `CONCAT(a, b)` for clarity with NULLs |
| Pattern match | `GLOB`, `REGEXP`, NOCASE `LIKE` | `LIKE`/`ILIKE`/`~`/`SIMILAR TO` |
| Catalog | `sqlite_master` | `information_schema.tables` / `pg_catalog.pg_tables` |
| Pragmas | `PRAGMA ...` | drop; equivalents are session settings or `SET` |
| Placeholders | `?` in raw `cursor.execute(...)` | go through `db_optimizer` (it adapts) — never raw `?` against psycopg2 |
| `DISTINCT` + `ORDER BY` | `SELECT DISTINCT ... ORDER BY col_not_in_select` | move to `GROUP BY` or include the ordering column in SELECT |
| `LIKE` placeholder escaping | literal `%` in SQL plus `?` placeholders | escape `%` → `%%` in the SQL before psycopg2 sees it (already handled in `core/postgres_compat.py`, but raw-SQL paths must use the adapter) |
| Stripe object leaks | returning `StripeObject`/`ListObject` to `jsonify` | flatten to plain `dict`/`list` (today's pattern) |
| Type strictness | mixing strings/ints in WHERE clauses against typed PG columns | cast explicitly, or fix upstream so the param matches the column type |
| Direct sqlite3 | `import sqlite3` / `sqlite3.connect(...)` in runtime modules | route everything through `db_optimizer` |
| Transactions | implicit auto-commit assumptions | use the optimizer's connection/cursor lifecycle |

## Inventory

> Status legend: ⏳ pending · 🔧 in progress · ✅ done · 🟡 needs schema decision · ⏸️ deferred · ❌ false positive

### Pattern summary (Phase 1 scan, 2026-05-10; refined 2026-05-10)

| Severity | Pattern | Hits |
|---|---|---:|
| high | `AUTOINCREMENT` | 33 |
| high | `strftime(` | 22 |
| high | `fromisoformat` on row column | 19 |
| high | `PRAGMA` | 3 |
| high | `cursor.lastrowid` | 2 |
| high | `raw sqlite3 import` | 2 |
| high | `last_insert_rowid()` | 1 |
| high | `sqlite3.connect(` | 1 |
| low | `SELECT DISTINCT` (advisory) | 3 |
| **Total** | | **86** |

**Refinement note**: The original scan reported 6 `boolean = integer literal` hits. After inspecting each, all 6 turned out to be Python variable initializations (`processed = 0`, `failed = 0`, etc.) — none were SQL. The scanner's regex was tightened to require a SQL context (`WHERE` / `AND` / `OR` / `SET` / comma) immediately preceding the comparison. All `granted = 1`-style SQL had already been converted to `= TRUE` in commit `338279ec`. **No remaining real bugs in this category.**

### Triage notes (initial pass, before code edits)

These are scanner hits I've already inspected and classified. The remaining rows in the table below will be triaged when their feature-area PR is opened.

- **`fromisoformat` false positives** — the scanner intentionally catches every `datetime.fromisoformat(<row>['col'])` candidate. Many existing call sites already have `isinstance(x, str)` guards (verified by reading):
  - `core/expert_escalation.py:257-259` — `isinstance(row['..._at'], str)` guard already in place — ❌ FP
  - `core/appointments_service.py:218-219, 422-423, 456-457` — `isinstance(..., datetime)`/`isinstance(..., str)` guards already in place — ❌ FP
  - `core/smart_faq_system.py:956,958` — `isinstance(data['..._at'], str)` guards already in place — ❌ FP
  - `services/business_operations.py:146,159` — operates on in-memory event dicts, not DB rows — ❌ FP
  - `routes/integrations.py:246-247, 283, 285` and `routes/appointments.py:252` and `crm/completion_api.py:110` — parse user-supplied JSON request strings, not DB rows — ❌ FP
- **`crm/service.py:727`** — `SELECT DISTINCT` already replaced with `GROUP BY` in commit `338279ec`; the scanner hits the **comment** explaining that fix. ❌ FP (we'll suppress with `# noqa: pg-audit`).
- **`core/privacy_manager.py:202`** — same: hits the explanatory **comment** added in `338279ec`, not active SQL. ❌ FP (will suppress).
- **`core/kpi_tracker.py:872`** and **`core/workflow_followups.py:372`** — DISTINCT without an out-of-list ORDER BY (verified). Manual review only.
- All **`AUTOINCREMENT`** hits live in `CREATE TABLE IF NOT EXISTS` bootstrap DDL. On the production PG database the tables already exist (so `IF NOT EXISTS` makes these effectively dead code in prod). But they ARE wrong DDL and would break any clean PG bring-up. Treated as 🟡 — see "Open schema questions" below.
- **`strftime(` × 22 — ALL false positives** (verified 2026-05-10). Every flagged line is a Python `datetime.strftime()` or `time.strftime()` method call producing a string for a filename / dict key / log path / Gmail query / parameterized SQL value. Whole-tree audit (`(?<!\.)strftime\(` across `.py/.sql/.sh/.js/.ts/.yml`) found exactly one non-method `strftime(` occurrence, and it was the scanner's own pattern literal. The codebase has zero SQL `strftime()` calls. Scanner regex now uses `(?<!\.)` negative lookbehind to skip the method-call form. Confirmed sites and their downstream uses:
  - `analytics/dashboard_api.py:823-824, 958` — produces `month_start`, `month_key`, dict `'date'` value. No SQL contact.
  - `core/ai_budget_guardrails.py:77`, `core/public_chatbot_api.py:83`, `core/tier_usage_caps.py:79,107` — `month = datetime.now(...).strftime("%Y-%m")` used as a parameterized query value (`WHERE month = ?`). Backend column is TEXT, comparison is string equality. PG-safe.
  - `core/api_key_manager.py:331` — `since_str` passed as parameter to `WHERE created_at >= ?`. PG coerces ISO-format TEXT to timestamp; works on either column type.
  - `core/billing_api.py:1181,1203` — `month` key for `billing_usage` TEXT column. PG-safe.
  - `core/docs_forms_api.py:541`, `core/enterprise_logging.py:39`, `core/webhook_intake_service.py:239`, `core/document_templates_system.py:494-526` — filenames, ID seeds, display strings. No SQL contact.
  - `core/stripe_webhooks.py:811`, `email_automation/gmail_sync_jobs.py:653`, `integrations/outlook/outlook_sync.py:175` — produces external-API query strings (Stripe, Gmail, Outlook). No SQL contact.

### Generated inventory (raw scanner output)

> Regenerate any time with `python3 scripts/check_postgres_sql_patterns.py --format markdown`.

_Generated by `scripts/check_postgres_sql_patterns.py` — 92 hits across 14 feature areas._

| Feature | File | Line | Severity | Pattern | Fix hint |
|---|---|---:|---|---|---|
| Analytics | `analytics/dashboard_api.py` | 823 | high | strftime( | Use `to_char()`, `EXTRACT()`, or `date_trunc()`. |
| Analytics | `analytics/dashboard_api.py` | 824 | high | strftime( | Use `to_char()`, `EXTRACT()`, or `date_trunc()`. |
| Analytics | `analytics/dashboard_api.py` | 958 | high | strftime( | Use `to_char()`, `EXTRACT()`, or `date_trunc()`. |
| Analytics | `core/kpi_tracker.py` | 872 | low | SELECT DISTINCT | Manual review (verified clean). |
| App entry | `scripts/prestart.py` | 9 | high | raw sqlite3 import | Route DB access through `db_optimizer` (which adapts to PG in prod). |
| App entry | `scripts/prestart.py` | 42 | high | sqlite3.connect( | Route through `db_optimizer` so the connection respects the configured DB. |
| App entry | `scripts/prestart.py` | 47 | high | PRAGMA | Drop the PRAGMA. Use session-level `SET` or connection options if needed. |
| App entry | `scripts/prestart.py` | 48 | high | PRAGMA | Drop the PRAGMA. |
| App entry | `scripts/prestart.py` | 49 | high | PRAGMA | Drop the PRAGMA. |
| App entry | `scripts/prestart.py` | 65 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| App entry | `scripts/prestart.py` | 81 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| App entry | `scripts/prestart.py` | 97 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| App entry | `scripts/prestart.py` | 110 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| App entry | `scripts/prestart.py` | 123 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| App entry | `scripts/prestart.py` | 137 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Auth & Sessions | `core/api_key_manager.py` | 39 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Auth & Sessions | `core/api_key_manager.py` | 62 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Auth & Sessions | `core/api_key_manager.py` | 331 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Auth & Sessions | `core/jwt_auth.py` | 87 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Auth & Sessions | `core/secure_sessions.py` | 109 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Automations | `core/automation_run_events.py` | 47 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Automations | `core/automation_safety.py` | 40 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Automations | `core/automation_safety.py` | 58 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Automations | `core/automation_safety.py` | 76 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Automations | `core/expert_escalation.py` | 257 | high | fromisoformat | ❌ FP — guarded with `isinstance(..., str)` already. |
| Automations | `core/expert_escalation.py` | 258 | high | fromisoformat | ❌ FP — guarded. |
| Automations | `core/expert_escalation.py` | 259 | high | fromisoformat | ❌ FP — guarded. |
| Automations | `services/automation_engine.py` | 1378 | high | boolean = integer literal | Replace with `= TRUE` / `= FALSE`. |
| Automations | `services/automation_queue.py` | 69 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| CRM | `crm/completion_api.py` | 110 | high | fromisoformat | ❌ FP — parses request input. |
| CRM | `crm/service.py` | 727 | low | SELECT DISTINCT | ❌ FP — comment text, not SQL. |
| Email Automation | `email_automation/gmail_sync_jobs.py` | 141 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Email Automation | `email_automation/gmail_sync_jobs.py` | 161 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Email Automation | `email_automation/gmail_sync_jobs.py` | 226 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Email Automation | `email_automation/gmail_sync_jobs.py` | 262 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Email Automation | `email_automation/gmail_sync_jobs.py` | 653 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Email Automation | `email_automation/jobs.py` | 535 | high | boolean = integer literal | Replace with `= TRUE` / `= FALSE`. |
| Infra | `core/enterprise_logging.py` | 39 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Infra | `core/idempotency_manager.py` | 81 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Infra | `core/rate_limiter.py` | 136 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Infra | `core/rate_limiter.py` | 157 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `core/integrations/integration_framework.py` | 98 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `core/integrations/integration_framework.py` | 115 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `core/integrations/integration_framework.py` | 131 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `core/integrations/integration_framework.py` | 148 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `core/oauth_token_manager.py` | 74 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `core/oauth_token_manager.py` | 95 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Integrations | `integrations/outlook/outlook_sync.py` | 175 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc | `services/business_operations.py` | 146 | high | fromisoformat | ❌ FP — in-memory dict, not DB. |
| Misc | `services/business_operations.py` | 159 | high | fromisoformat | ❌ FP — in-memory dict. |
| Misc (core) | `core/ai_budget_guardrails.py` | 46 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Misc (core) | `core/ai_budget_guardrails.py` | 61 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Misc (core) | `core/ai_budget_guardrails.py` | 77 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/appointments_service.py` | 127 | high | cursor.lastrowid | Use `INSERT ... RETURNING id`. |
| Misc (core) | `core/appointments_service.py` | 218 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/appointments_service.py` | 219 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/appointments_service.py` | 422 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/appointments_service.py` | 423 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/appointments_service.py` | 456 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/appointments_service.py` | 457 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/billing_api.py` | 250 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Misc (core) | `core/billing_api.py` | 264 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Misc (core) | `core/billing_api.py` | 1170 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/billing_api.py` | 1192 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/chatbot_content_events.py` | 131 | high | cursor.lastrowid | Use `INSERT ... RETURNING id`. |
| Misc (core) | `core/docs_forms_api.py` | 541 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/document_templates_system.py` | 494 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/document_templates_system.py` | 495 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/document_templates_system.py` | 500 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/document_templates_system.py` | 502 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/document_templates_system.py` | 504 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/document_templates_system.py` | 526 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/public_chatbot_api.py` | 83 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/smart_faq_system.py` | 956 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/smart_faq_system.py` | 958 | high | fromisoformat | ❌ FP — guarded. |
| Misc (core) | `core/tier_usage_caps.py` | 79 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/tier_usage_caps.py` | 107 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/user_feedback_router.py` | 14 | high | raw sqlite3 import | Route through `db_optimizer`. |
| Misc (core) | `core/webhook_api.py` | 44 | high | last_insert_rowid() | Use `INSERT ... RETURNING id`. |
| Misc (core) | `core/webhook_intake_service.py` | 239 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |
| Misc (core) | `core/workflow_followups.py` | 166 | high | boolean = integer literal | Replace with `= TRUE` / `= FALSE`. |
| Misc (core) | `core/workflow_followups.py` | 167 | high | boolean = integer literal | Replace with `= TRUE` / `= FALSE`. |
| Misc (core) | `core/workflow_followups.py` | 372 | low | SELECT DISTINCT | Manual review (verified clean — `DISTINCT user_id`, no ORDER BY). |
| Onboarding | `core/app_onboarding.py` | 50 | high | AUTOINCREMENT | 🟡 see schema question Q1 |
| Onboarding | `core/onboarding_jobs.py` | 241 | high | boolean = integer literal | Replace with `= TRUE` / `= FALSE`. |
| Privacy | `core/privacy_manager.py` | 202 | high | boolean = integer literal | ❌ FP — comment text from prior fix. |
| Routes (misc) | `routes/appointments.py` | 252 | high | fromisoformat | ❌ FP — parses request input. |
| Routes (misc) | `routes/integrations.py` | 246 | high | fromisoformat | ❌ FP — parses request input. |
| Routes (misc) | `routes/integrations.py` | 247 | high | fromisoformat | ❌ FP — parses request input. |
| Routes (misc) | `routes/integrations.py` | 283 | high | fromisoformat | ❌ FP — parses request input. |
| Routes (misc) | `routes/integrations.py` | 285 | high | fromisoformat | ❌ FP — parses request input. |
| Webhooks | `core/stripe_webhooks.py` | 811 | high | strftime( | Replace with `to_char()` / `EXTRACT()`. |

### After-triage real-bug count (post-scanner-refinement, post-RETURNING-id PR)

| Category | True positives | Status |
|---|---:|---|
| `AUTOINCREMENT` (DDL bootstrap) | 27 | ⏳ pending — covered by Q1 plan (was 33; 6 closed by prestart rewrite) |
| `strftime(` in SQL | **0** | ✅ done (all 22 were FP — every site is a Python `datetime.strftime()` or `time.strftime()` method call producing a string used as a filename/dict key/query parameter, **not** SQLite's SQL `strftime()` function. Scanner regex tightened with `(?<!\.)` negative lookbehind to skip method-call form.) |
| `PRAGMA` (sqlite-only init) | **0** | ✅ done (was 3; closed by prestart rewrite) |
| `raw sqlite3 import` | **0** | ✅ done (was 2; closed by prestart rewrite + `core/user_feedback_router.py` refactor) |
| `sqlite3.connect(` | **0** | ✅ done (was 1; closed by prestart rewrite) |
| `cursor.lastrowid` | **0** | ✅ done (was 2; closed by commit `2dcd9d7a` + `14155f71`) |
| `last_insert_rowid()` | **0** | ✅ done (was 1; closed by commit `caf9b088`) |
| `boolean = integer literal` | 0 | ✅ done (all 6 were FP; scanner tightened in `a89de049`) |
| `fromisoformat` on row column | 0 | ✅ done (all 19 were FP — already-guarded or non-DB) |
| `SELECT DISTINCT` (advisory) | 0 | ✅ done (all 3 verified clean) |
| **Total non-DDL true positives remaining** | **0** | (strftime category collapsed to 0 after regex tightening) |
| **Total DDL (AUTOINCREMENT) remaining** | **27** | |

## Open schema questions — RESOLVED

These were answered 2026-05-10. Decisions baked into the PR plan:

- **Q1 — AUTOINCREMENT**: option (a) — replace with `BIGSERIAL PRIMARY KEY` in all 33 places, no ALTERs to existing prod tables. Fresh-DB bootstrap now works; prod behavior unchanged.
- **Q2 — Booleans-as-INTEGER**: option (b) — this audit only fixes the comparison operators (`= 1` → `= TRUE`). Column-shape audit deferred.
- **Q3 — TEXT-storing-timestamps**: option (a) — discovery script now, append findings to this doc, decide per-table later.

### Q1 — `AUTOINCREMENT` in `CREATE TABLE IF NOT EXISTS` bootstrap statements

Across 14 files we still emit DDL that says:

```sql
CREATE TABLE IF NOT EXISTS foo (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ...
);
```

On the production PG database these tables already exist, so `IF NOT EXISTS` keeps the bootstrap a no-op today. But:

1. The DDL is **wrong** — if someone bootstraps a fresh PG database (staging, disaster recovery, a new dev DB), every one of these CREATE TABLE will fail with `syntax error at or near "AUTOINCREMENT"`.
2. There's no guarantee the prod tables' `id` columns are actually `BIGSERIAL` / `IDENTITY`. They may have been created by an early Alembic-style run with a different shape, or with a sequence that's now out of sync with `max(id)`.

Proposed direction (need approval before doing):
- **(a) Make the DDL PG-correct.** Replace `INTEGER PRIMARY KEY AUTOINCREMENT` with `BIGSERIAL PRIMARY KEY` in every `CREATE TABLE IF NOT EXISTS`. Existing prod tables untouched. Net effect: no behavior change in prod, but fresh-DB bootstrap now works.
- **(b) Same as (a), PLUS** a one-shot diagnostic script that inspects production PG and reports any `id` column that ISN'T a `BIGSERIAL`/`IDENTITY` (so we can decide whether to ALTER them).
- **(c) Defer entirely.** Mark these as known-warn, no DDL changes; revisit when we adopt Alembic.

### Q2 — Booleans in `INTEGER` columns

Several boolean fields (`is_active`, `granted`, `onboarding_completed`, `enabled`, etc.) are declared as `INTEGER` in the bootstrap DDL but used as booleans throughout the code. This is why we keep hitting `operator does not exist: boolean = integer` errors — the column might actually be `BOOLEAN` in prod PG (created by SQLAlchemy at some point) but our DDL says `INTEGER`.

This is detected indirectly via the `boolean = integer literal` scanner hits — every one of those is a symptom of "code thinks the column is boolean, but our DDL says integer." Today's audit fixes the comparisons. The real question:

- **(a) Standardize on `BOOLEAN`** in DDL and code. Audit the prod schema (script) and decide per-table whether an ALTER is needed.
- **(b) Standardize on `INTEGER 0/1`** everywhere (treat as bit flag). Update code paths that use `= TRUE` / `= FALSE` to use `= 1` / `= 0` — except we know PG rejects that against BOOLEAN columns, so this would only work if the columns truly are INTEGER.
- **(c) Keep current state** — fix the comparison operators (this audit), defer the schema-shape question.

### Q3 — TEXT columns storing timestamps

We already hit this once: `automation_jobs.created_at` is `TEXT` but compared against `TIMESTAMPTZ`. We added `_pg_ts()` casts as a workaround. Same pattern may exist on other tables. Worth a discovery script?

#### Q3 discovery (auto-generated 2026-05-10)

Static scan of every `CREATE TABLE IF NOT EXISTS` in the codebase. Live scan against prod PG is opt-in via `DATABASE_URL=... python3 scripts/discover_pg_schema_drift.py --live`.

**Tables discovered in codebase DDL:** 87.

**High-severity drift — timestamp-named columns declared as TEXT (the same bug class as `automation_jobs.created_at`):**

| Table | Column | Declared type | Notes |
|---|---|---|---|
| `automation_jobs` | `completed_at` | `TEXT` | **(migration prepared 2026-05-10)** Static usage audit found INSERTs only + `IS NOT NULL` + `dt.fromisoformat()` in Python. No SQL WHERE comparison against a timestamp. Safe today but latent. Promote to `timestamptz` in migration 002. |
| `automation_jobs` | `created_at` | `TEXT` | Already cast via `_pg_ts()` in commit `47f7099a`. **(migration prepared 2026-05-10)** Promote to `timestamptz NOT NULL DEFAULT now()` in migration 002; cast becomes a no-op. |
| `automation_jobs` | `started_at` | `TEXT` | **(migration prepared 2026-05-10)** Same usage profile as `completed_at`. Promote to `timestamptz` in migration 002. |
| `automation_run_events` | `created_at` | `TEXT` | **(migration prepared 2026-05-10)** Used in INSERTs + `SELECT ... ORDER BY id DESC` (not by created_at). No filter against timestamp. Promote to `timestamptz NOT NULL DEFAULT now()` in migration 002. |
| `customer_appointment_intake_submissions` | `requested_date` | `TEXT` | **(migration prepared 2026-05-10)** Verified INSERT-only, no WHERE filter anywhere. Promote to `date` in migration 002. |
| `customer_appointment_intake_submissions` | `requested_time` | `TEXT` | **(migration prepared 2026-05-10)** Verified INSERT-only, no WHERE filter anywhere. Promote to `time` in migration 002. |

**Live verification (Supabase MCP, 2026-05-10):** all 4 affected tables hold **zero rows** at audit time. `ALTER COLUMN TYPE` is therefore a no-cost rewrite — no data risk, no downtime concern, only one dependent index (`idx_automation_run_events_user_created`) which Postgres rebuilds automatically on an empty table.

**Migration file:** `scripts/migrations/002_pg_audit_promote_text_timestamps.sql` (BEGIN/COMMIT-wrapped, ready to paste into Supabase SQL editor). Awaiting application by the user (Supabase MCP is currently in read-only mode in this workspace).

**Coupled code changes** (already committed in the same PR):
- `services/automation_queue.py`: introduced `_row_value_as_iso(v)` and `_row_value_as_datetime(v)` helpers to make 2 read sites shape-tolerant. They handle both psycopg2-returns-str (legacy TEXT column) and psycopg2-returns-datetime (post-migration TIMESTAMPTZ column). API response contract preserved (`created_at`/`started_at`/`completed_at` keep returning ISO strings to the frontend). P95 duration calculation no longer silently fails after the migration.
- CREATE TABLE DDL updated in `services/automation_queue.py`, `core/automation_run_events.py`, and `core/database_optimization.py` so fresh PG bring-ups (dev / staging / DR) create the correct types from day one. SQLite still accepts these type names (NUMERIC affinity for unknown tokens, DATE/TIME are NUMERIC affinity), verified with an in-memory smoke test.
- `db_optimizer._pg_ts()` and the `sql_column_newer_than_*()` / `sql_timestamp_*()` helpers are left **unchanged**. They emit `(col)::timestamptz`, which is a no-op against a real `timestamptz` column. The helpers remain as defensive shims for any future TEXT-typed `_at` column the codebase may introduce.

**Medium-severity drift — boolean-named columns declared as INTEGER:**

Static heuristic flagged two, both verified ❌ false positive: `data_retention_logs.records_deleted` and `email_sync.emails_processed` are **counts**, not booleans. The heuristic is intentionally aggressive; no fix needed.

**Low-severity drift — JSON-named columns declared as TEXT:** 41 columns. Out of scope for this audit (functional today; native JSONB is a future optimization, not a bug).

## Q4 — Broader sweep (things the regex scanner can't catch)

Completed 2026-05-10. Systematic check for PG-vs-SQLite divergences that aren't pattern-grep-able from a SQL fragment alone — branched DDL paths, raw cursor leaks, SQLite-specific catalog queries, mixed-case identifiers, JSON access patterns, and surrounding Supabase-platform health.

### Real bugs found and fixed in this pass

| Finding | Severity | Where | Fix | Status |
|---|---|---|---|---|
| `core/database_optimization.get_database_stats()` always queried `sqlite_master` + ran `PRAGMA page_count/page_size`. Would 500 on PG with `relation sqlite_master does not exist`. Zero callers today, so dormant — but the next monitoring/health-check route to wire it up would have failed in prod. | latent high | `core/database_optimization.py:2629` | Added PG branch using `information_schema.tables` + `information_schema.statistics` for table+index inventory, `pg_total_relation_size()` aggregated over `pg_class` for size. Kept SQLite path for local dev. Smoke-tested: 88 tables, 36 MB size on local SQLite. | ✅ this turn |

### Verified-safe (audited, no work needed)

The following patterns initially looked suspicious but turned out to be correctly branched. Logged here so future audits don't re-investigate.

| Pattern | Location | Why it's safe |
|---|---|---|
| `INSERT OR REPLACE INTO ...` (4 sites) | `core/database_optimization.py:2891, 3048, 3074, 3134` | All inside `else:` branches of `if self.db_type == "postgresql"` blocks; PG path uses `INSERT ... ON CONFLICT (...) DO UPDATE SET ...`. |
| `INSERT OR IGNORE INTO users` (3 sites) | `tests/test_api_key_manager.py:33`, `tests/test_api_key_manager_allowed_origins.py:38`, `tests/test_integration_framework.py:55,146` | Test-only setup against in-memory SQLite. Phase 3 (tests on PG) will revisit. |
| `sqlite_master` queries (3 sites) | `core/database_optimization.optimize_database()`, `core/database_optimization.table_exists()`, `core/database_optimization.list_table_columns()` | All correctly branch on `db_type == "postgresql"` — PG paths use `information_schema` instead. |
| `PRAGMA journal_mode=WAL` / `synchronous=NORMAL` / `foreign_keys=ON` | `core/database_optimization.py:198, 2052-2056, 2513` | All inside SQLite-only connection setup (the `else` branch of the connection-pool guard). PG connections never see PRAGMA. |
| `json_extract(col, '$.path')` | `core/database_optimization.json_field_expr()` (only call site) | Helper branches: PG returns `(col::jsonb->>'path')`, SQLite returns `json_extract(col, '$.path')`. |
| `ALTER TABLE ... ADD COLUMN ... BOOLEAN DEFAULT 0` (multiple sites) | `core/database_optimization.py:341, 195, …`, `email_automation/gmail_sync_jobs.py:195, 210` | All route through `db_optimizer.execute_query()` → `should_translate_sqlite_ddl()` → `translate_sqlite_ddl_to_postgres()` which rewrites `BOOLEAN DEFAULT 0` to `BOOLEAN DEFAULT FALSE`. |
| `payload_truncated BOOLEAN` value inserts | `routes/appointments.py::_stringify_payload()` returns `(str, bool)` — a Python `bool` | psycopg2 maps Python `bool` → PG `boolean` natively; SQLite stores 0/1. Both correct. |
| `ALTER TABLE refresh_tokens ADD COLUMN device_id TEXT` | `core/jwt_auth.py:106` | Routes through `db_optimizer.execute_query()`. PG accepts the statement unchanged. |
| Raw `cursor.execute(...)` calls outside `db_optimizer` | `core/appointments_service.py:105/118/228`, `core/chatbot_content_events.py` | All inside `db_optimizer.transaction()` context manager, which on PG wraps the cursor with `PostgresBootstrapCursor` (translates `?` → `%s` and inline DDL). |
| Identifier casing | full source tree | Consistent `lower_snake_case` throughout. No quoted mixed-case identifiers. Zero PG-vs-SQLite case-folding risk. |
| `FOR UPDATE` / `FOR SHARE` / `SKIP LOCKED` / `NOWAIT` | not used anywhere | Confirmed via grep. Worth considering for the `automation_jobs` worker (PG would support `SELECT ... FOR UPDATE SKIP LOCKED` for safe concurrent dequeue), but that's a future optimization, not a portability bug. |
| `EXTRACT(...)`, `TRUNCATE TABLE`, `NULLS FIRST/LAST`, `LISTEN/NOTIFY`, `pg_advisory_lock`, `ARRAY[...]`, `regexp_match/replace`, `::int[]`, `SIMILAR TO` | not used anywhere | Confirmed via grep. Codebase is conservative — no PG-only SQL features in active use beyond `ON CONFLICT`, `RETURNING id`, and the helpers in `db_optimizer`. |
| `EXTRACT(EPOCH FROM ...)` / `julianday()` / `sqlite_version()` | not used | Confirmed via grep. |
| `isolation_level` / `autocommit` / `set_session` | not configured | Confirmed via grep. Backend uses psycopg2's default isolation (READ COMMITTED) and per-query `conn.commit()`. SQLite uses default (auto-transaction per statement). Both correct for the connection-per-request pattern. |
| `INSERT OR REPLACE` semantics differ on conflict | n/a | Both backends preserve insertion order; PG `ON CONFLICT DO UPDATE` matches SQLite's REPLACE for the conflict-then-update path. Branches confirmed above. |

### Orphan scripts (manually-run, SQLite-only, NOT on production path)

| Script | Status |
|---|---|
| `scripts/migrations/phase1_migrations.py` | Imports `sqlite3` directly, hardcoded `sqlite3.connect("data/fikiri.db")`. Never imported from any other module. Dead utility from the leads-pipeline build. **Same shape as the old `prestart.py`** — kept for historical local-dev runs; no production reach. |
| `scripts/migrations/001_add_user_id_to_leads.py` | Same — direct `sqlite3` import, zero callers. The user_id-on-leads migration was already applied to prod elsewhere. |

**Decision deferred:** these can stay since they're harmless documentation of the historical migration steps. If we ever revisit, the right move is either to (a) delete them, or (b) rewrite them as Supabase migration SQL files like `scripts/migrations/002_pg_audit_promote_text_timestamps.sql`.

### Parallel concerns surfaced via Supabase advisors (NOT in scope for this audit)

Logged here so they don't get lost. Each is its own follow-up.

- **`get_advisors(security)` → 86 INFO findings, all `rls_enabled_no_policy`**: 86 public tables have row-level security enabled but no policies defined. Currently survived because the Flask backend connects via the Supabase service role (which bypasses RLS). If anyone ever wires up an anon/authenticated Supabase REST/RPC client, queries against these tables would return zero rows. Confirm intent (service-role-only is fine; otherwise add policies).
- **`get_advisors(performance)` → 158 INFO findings**: 132 `unused_index` (most tables have low row counts today; revisit when each table exceeds ~10k rows) and 26 `unindexed_foreign_keys` (low-severity perf hygiene; mostly on cascade-delete paths that will rarely fire).
- **Installed extensions**: `plpgsql`, `pgcrypto`, `uuid-ossp`, `pg_stat_statements`, `supabase_vault`. Conservative — no `pg_jsonschema` (could validate JSON columns), no `pg_cron` (could replace some scheduled jobs), no `vector` (would be needed if we ever go beyond OpenAI-hosted embeddings).

## Proposed PR sequence

In order from lowest-risk and highest-blast-radius down to riskiest. Each PR closes a slice of the inventory.

| # | PR | Scope | Schema touch? | Inventory rows closed |
|---|---|---|---|---|
| 1 | `pg-audit(scanner): widen + lint` | Already done on this branch — the new scanner itself. ✅ | no | n/a |
| 2 | `pg-audit(returning-id): replace lastrowid + last_insert_rowid` | Done. Added `db_optimizer.transaction()` + made `PostgresBootstrapCursor.execute()` return `self` (enables `.execute(...).fetchall()` chain on PG). Migrated `core/chatbot_content_events.py` (9 transactions, all silently broken on PG previously), `core/appointments_service.py` (2 transactions), `core/webhook_api.py` (drop broken `_last_insert_rowid()`). Tests updated. ✅ | no | 3 (scanner) / 12 (actual reach) |
| 3 | `pg-audit(strftime): replace with to_char/EXTRACT` | Investigated. **No work needed.** Whole-tree audit (`(?<!\.)strftime\(` across `.py/.sql/.sh/.js/.ts/.yml`) finds exactly 1 non-method `strftime(` occurrence — and it's the literal pattern definition inside `scripts/check_postgres_sql_patterns.py:144`. The 22 scanner hits were 100% Python `datetime.strftime()` / `time.strftime()` method calls producing strings used as filenames, dict keys, log paths, and parameterized SQL values. None embed into a SQL `strftime()` function call. Scanner tightened to skip the method-call form. ✅ | no | 22 (all FP — closed by scanner-regex fix) |
| 4 | `pg-audit(prestart): replace raw sqlite path` | Done. Rewrote `scripts/prestart.py` as a thin Postgres-aware shim that delegates schema bootstrap to `db_optimizer` (which already runs the bootstrap synchronously on import). Dropped `import sqlite3`, `sqlite3.connect(...)`, three `PRAGMA` statements, and six `CREATE TABLE ... AUTOINCREMENT` blocks. Lockfile (`data/.db_init_lock`) interface preserved. **Note** — script was never wired into production (`render.yaml`/`Dockerfile`/`app.py` don't invoke it). It's a manual dev/orchestrator hook now, made PG-safe. ✅ | no | 11 (6 AUTOINCREMENT, 3 PRAGMA, 1 sqlite3.connect, 1 raw import) |
| 5 | `pg-audit(feedback-router): route through db_optimizer` | Done. Dropped `import sqlite3` from `core/user_feedback_router.py`. Replaced the driver-specific `except sqlite3.OperationalError` in the retry loop with a portable message-based check that also matches psycopg2's `deadlock detected` / `lock timeout` messages. `db_optimizer.execute_query()` already handles `?`→`%s` adaptation, so no SQL changes were needed. ✅ | no | 1 |
| 6 | `pg-audit(autoincrement): PG-correct DDL` | Replace `AUTOINCREMENT` with `BIGSERIAL` in 27 remaining CREATE TABLE statements per Q1.a. No ALTERs to prod. | yes (idempotent DDL only) | 27 |
| 7 | `pg-audit(ci-gate)` | Promote `scripts/check_postgres_sql_patterns.py --fail` into CI. Add `# noqa: pg-audit` suppressions to the 3 advisory `SELECT DISTINCT` hits (verified clean) so CI stays green. | no | 3 |

After all PRs land:
- Re-run scanner → expect **0** hits (with FPs suppressed).
- Promote scanner to CI (`--fail`) so we don't regress.

## Change log

> One row per landed change. New entries go at the top.

| Date (UTC) | Commit | Feature area | File(s) | What | Why |
|---|---|---|---|---|---|
| 2026-05-10 | `0c1e42ff` | DB infra / Audit | `core/database_optimization.py`, `docs/POSTGRES_MIGRATION_AUDIT.md` | Q4 broader sweep landed. (1) Fixed `get_database_stats()` — old code unconditionally queried `sqlite_master` and ran `PRAGMA page_count/page_size`, which would 500 on PG. Added a PG branch using `information_schema.tables`, `information_schema.statistics`, and `pg_total_relation_size()` aggregated over `pg_class`. SQLite path preserved for local dev. Smoke test: 88 tables, 36 MB on local SQLite. (2) Audited everything the regex scanner can't see — branched DDL paths, raw cursor leaks, `sqlite_master` queries, `INSERT OR REPLACE` paths, PRAGMA setup, JSON access, identifier casing, transaction isolation, `FOR UPDATE` / `LISTEN/NOTIFY` / `pg_advisory_lock` / `EXTRACT(EPOCH FROM ...)` / `NULLS FIRST/LAST` / `ARRAY[]` / `regexp_*` / `julianday()` — codebase is **clean** on every one of these. Logged the verified-safe inventory in the audit doc so future audits don't redo the work. (3) Pulled live Supabase advisor data: 86 `rls_enabled_no_policy` INFO findings (backend uses service-role, so no immediate impact) and 158 perf advisories (132 unused indexes on near-empty tables + 26 unindexed FKs). Both filed as parallel concerns, NOT in PG-migration scope. Confirmed installed PG extensions: `pgcrypto`, `uuid-ossp`, `pg_stat_statements`, `supabase_vault`, `plpgsql`. | "No workarounds" rule says: prove the codebase is clean for every PG-vs-SQLite divergence we know about. The scanner catches pattern-grep-able diffs; the broader sweep catches structural ones (branched paths, helper escapes, surrounding platform state). Net: one real latent bug fixed, dozens of patterns verified-safe, parallel Supabase-platform concerns surfaced for follow-up. |
| 2026-05-10 | `f35e3961` | Schema / Automation | `scripts/migrations/002_pg_audit_promote_text_timestamps.sql` (new), `services/automation_queue.py`, `core/automation_run_events.py`, `core/database_optimization.py`, `docs/POSTGRES_MIGRATION_AUDIT.md` | Q3 schema-drift resolution. **Live audit (Supabase MCP) confirmed** that the 6 high-severity TEXT-typed temporal columns identified by `discover_pg_schema_drift.py` all live in **empty production tables** (zero rows in `automation_jobs`, `automation_run_events`, `customer_appointment_intake_submissions`). Authored a single ALTER migration (file 002) that promotes them to native `timestamptz` / `date` / `time` and adds `DEFAULT now()` on the two `NOT NULL created_at` columns. Migration is **prepared but not yet applied** — Supabase MCP is read-only in this workspace; user runs it via dashboard / SQL editor / write-mode MCP. **Coupled code changes shipped now** so the application is shape-tolerant pre- and post-migration: added `_row_value_as_iso(v)` and `_row_value_as_datetime(v)` to `services/automation_queue.py`, applied them to the two read sites (`get_job_status()` API dict + p95 duration parser). Updated CREATE TABLE DDL in 3 files so fresh PG bring-ups get the right types from day one. SQLite still accepts the new type names (verified with in-memory smoke test). 17 affected unit tests pass. `_pg_ts()` and `sql_column_newer_than_*()` helpers are unchanged — they become no-ops on the new TIMESTAMPTZ columns, retained as defensive shims for any future TEXT-typed `_at` column. | Closes the audit's Q3 question (TEXT-storing-timestamps) at the schema layer rather than papering over it with `_pg_ts()` casts at every call site — directly aligned with the project's "no workarounds" rule. Critical detail: the only existing problematic site (`automation_jobs.created_at`) had a workaround in place since `47f7099a`; the other 5 were latent bugs (any future query like `WHERE completed_at > NOW() - INTERVAL '1 hour'` would have 500'd). Doing this now while the tables are empty is free; doing it later under load is a coordinated downtime + dual-write window. |
| 2026-05-10 | `a5f1024e` | Audit infra | `scripts/check_postgres_sql_patterns.py`, `docs/POSTGRES_MIGRATION_AUDIT.md` | Tightened the `strftime(` scanner regex with `(?<!\.)` negative lookbehind to exclude Python's `datetime.strftime()` / `time.strftime()` / any `module.strftime()` method-call form. **Outcome**: the `strftime` category collapsed from 22 hits to 0. Whole-tree audit across `.py/.sql/.sh/.js/.ts/.yml` confirms exactly 1 non-method `strftime(` occurrence in the repo, and it is the scanner's own pattern literal at `scripts/check_postgres_sql_patterns.py:144`. **Codebase has zero SQL `strftime()` calls.** Triage notes and PR-3 row updated. After-triage real-bug count: non-DDL TPs = 0, DDL AUTOINCREMENT = 27. | No-workarounds principle says don't shotgun fix things that aren't broken. All 22 sites were Python method calls producing strings used as filenames, dict keys, log paths, parameterized SQL values, or external-API queries — none embed into SQL. The right fix is to teach the scanner the difference, not to rewrite working code. |
| 2026-05-10 | `2e643832` | Billing | `core/billing_api.py`, `core/fikiri_stripe_manager.py`, `tests/test_billing_stripe.py` | `_db_row_scalar()` helper coerces psycopg2 `RealDict` rows AND sqlite3 tuple/Row rows so `get_user_email`/`get_user_name`/`get_stripe_customer_id` stop KeyError-ing on production PG. Rewrote `FikiriStripeManager.get_customer_details()` to read every Stripe field via `.get()` (StripeObject subclasses dict) and to drop the unsupported `expand=['default_source', ...]` parameter; result is now JSON-serializable. Two new unit tests pin the PG-dict and SQLite-tuple paths. | Customer billing details were 500-ing on PG because: (a) `users` query rows came back as RealDict (`row[0]` raised KeyError), and (b) the Stripe customer payload contained `StripeObject` instances that `jsonify()` can't encode. Sibling to commit `338279ec`. |
| 2026-05-10 | `9f5f58d6` | Feedback persistence | `core/user_feedback_router.py` | Drop `import sqlite3`. Replace driver-specific `except sqlite3.OperationalError` in `_execute_with_retry()` with a portable message-token check (`locked`, `busy`, `deadlock`, `lock timeout`) so both SQLite and psycopg2 transient-lock errors retry uniformly. INSERT goes through `db_optimizer.execute_query()`, which already adapts `?`→`%s`. **Deferred follow-up**: `record_feedback_event()` returns `cursor.rowcount` as `event_id`. Cosmetic on PG (mislabeled key), but real callers should switch to `INSERT ... RETURNING id` via `db_optimizer.execute_insert_returning_id()` — tracked separately, no current consumer reads the `event_id`. | sqlite3 import was the last `import sqlite3` in runtime source. Once gone, the scanner reports zero raw driver imports. |
| 2026-05-10 | `9f5f58d6` | Boot/prestart | `scripts/prestart.py` | Rewrote prestart from a 320-line SQLite-only bootstrapper into a 95-line Postgres-aware shim. New version: imports `core.database_optimization.db_optimizer` (which auto-runs `_initialize_database()` on import, PG-safe via `PostgresBootstrapCursor`), writes `data/.db_init_lock` on success, exits 0/1 like before. Removed all `sqlite3` imports, three `PRAGMA` calls, six `CREATE TABLE ... AUTOINCREMENT` DDL blocks, and a manual `sqlite3.connect("data/fikiri.db")`. **Audit note** — confirmed the script is **not** invoked from `render.yaml`, `Dockerfile`, or `app.py`. It was silently bootstrapping a local SQLite file in dev that the prod app (PG) never read. Idempotency preserved: lockfile is checked first, single-process operators get the same "already initialized" path. | Eliminate the last SQLite-only entrypoint in the repo. Even though prestart is unused in prod, leaving SQLite-shaped DDL here was a footgun: someone running it locally would see a "successful" boot against SQLite, then push to PG and discover the schema was never created there. The new path executes the same code as production startup. |
| 2026-05-10 | `caf9b088` | Webhooks | `core/webhook_api.py`, `tests/test_webhook_form_intake_persistence.py` | Drop broken `_last_insert_rowid()` helper. Use `db_optimizer.execute_insert_returning_id()` for the form-intake row id. Reword comments in appointments_service and chatbot_content_events so the audit scanner doesn't re-flag explanatory docstrings. | Old helper called `SELECT last_insert_rowid()` which doesn't exist on PG — every webhook form intake on prod was silently dropping its intake row id. Also racy on SQLite because it opened a fresh connection. |
| 2026-05-10 | `2dcd9d7a` | Appointments | `core/appointments_service.py`, `core/postgres_compat.py`, `tests/test_appointments_service.py` | Migrate 2 raw-cursor transaction blocks to `db_optimizer.transaction()`. Use `INSERT ... RETURNING id` instead of `cursor.lastrowid`. Make `PostgresBootstrapCursor.execute()` return `self` so the SQLite-idiomatic `.execute(...).fetchall()` chain works on PG. Update test fakes. | Both blocks were triply broken on PG: `?` placeholders, chained `.execute(...).fetchall()`, and `cursor.lastrowid` (always None on psycopg2). The "Failed to retrieve created appointment None" branch would trigger on every appointment creation. |
| 2026-05-10 | `14155f71` | Chatbot persistence | `core/chatbot_content_events.py` | Migrate all 9 transaction blocks to `db_optimizer.transaction()`. Change `_insert_event_tx()` to use `INSERT ... RETURNING id` portably (PG always; SQLite ≥ 3.35). | Every chatbot event recorded since the PG migration was silently failing — `cursor.execute("...?...")` raises a psycopg2 syntax error, the surrounding `try/except` logs "non-fatal", and the event row never lands. Chatbot persistence + FAQ/KB hydration was broken. |
| 2026-05-10 | `9fb8a14b` | DB infra | `core/database_optimization.py` | Add `db_optimizer.transaction()` context manager. Yields `(conn, cursor)` where the cursor on PG is wrapped with `PostgresBootstrapCursor` (adapts `?` → `%s` and translates inline DDL). Auto-rollback on exception. | Multiple callers (appointments_service, chatbot_content_events) need atomic multi-statement transactions but were using raw `conn.cursor()`, which doesn't adapt placeholders. This is the canonical helper for that pattern. |
| 2026-05-10 | `a89de049` | Audit infra | `scripts/check_postgres_sql_patterns.py`, `docs/POSTGRES_MIGRATION_AUDIT.md` | Refined the boolean-literal scanner regex to require a SQL context (WHERE / AND / OR / SET / comma). All 6 prior hits were Python variable inits (`processed = 0`), not SQL. Audit doc inventory updated. | Reduce scanner false-positive rate — every boolean-literal hit was an FP, masking real-bug visibility. |
| 2026-05-10 | `18fdcf12` | Audit infra | `scripts/discover_pg_schema_drift.py` | New static + live discovery script for column-type drift (TEXT-as-timestamp, INTEGER-as-boolean, TEXT-as-JSON). Static mode scans 87 tables in the codebase; live mode hits `DATABASE_URL` and dumps `information_schema.columns`. Findings appended under Q3 in this doc. | Per Q3 decision — surface latent timestamp-as-TEXT columns before they bite the way `automation_jobs.created_at` did. |
| 2026-05-10 | _(pending)_ | Audit infra | `scripts/check_postgres_sql_patterns.py` | Rewrote the PG safety scanner: line-level reporting, multi-pattern per file, JSON + Markdown output, feature-area grouping, `# noqa: pg-audit` suppression, expanded from 5 → 24 pattern classes. | Existing scanner missed AUTOINCREMENT, strftime, lastrowid, PRAGMA, fromisoformat, etc. — the inventory it produced wasn't actionable. |
| 2026-05-10 | _(pending)_ | Audit infra | `docs/POSTGRES_MIGRATION_AUDIT.md` | Seeded the audit doc with locked decisions, scope, pattern catalog, scanner-generated inventory, triage notes, open schema questions, and proposed PR sequence. | Source-of-truth for the codebase-wide PG migration audit per user direction. |
| 2026-05-10 | `5abf093f` | Pre-audit hardening | `core/idempotency_manager.py`, `core/api_key_manager.py`, `services/automation_engine.py`, `core/fikiri_stripe_manager.py`, `core/oauth_token_manager.py`, `integrations/outlook/outlook_client.py` | Guard `fromisoformat()` against native psycopg2 `datetime` objects; flatten `StripeObject` from `get_subscription.items.data` to plain dicts. | Same bug class that broke Privacy/CRM/Billing earlier the same day; fixed proactively across remaining hot paths. |
| 2026-05-10 | `5313f58b` | Pre-audit hardening | `frontend/src/pages/CRM.tsx`, `frontend/src/pages/GmailConnect.tsx` | CRM lead-card score badge wrap fix; gate Gmail "Syncing… 1%" spinner on actual `syncing: true` (not on the idle `pending` fallback). | Reported UI regressions; frontend was misreading backend's idle state as active sync. |
| 2026-05-10 | `338279ec` | Pre-audit hardening | `core/privacy_manager.py`, `crm/service.py`, `core/fikiri_stripe_manager.py` | Coerce psycopg2 datetimes (`_coerce_dt`); switch `granted = 1/0` → `granted = TRUE/FALSE` on BOOLEAN columns; replace `SELECT DISTINCT … ORDER BY` with `GROUP BY … ORDER BY MAX(...)`; coerce `StripeObject` to `dict()` in `get_customer_details`. | Privacy page "Failed to load", CRM Gmail-leads sync 500, Billing customer-details 500 — all visible in Render logs. |
| 2026-05-10 | `47f7099a` | Pre-audit hardening | `crm/service.py`, `core/database_optimization.py` | Postgres timestamp coercion in CRM; cast TEXT-typed timestamp columns via `_pg_ts()` helper in SQL window predicates. | `services.automation_queue` 500: `operator does not exist: text > timestamp with time zone`. |

## Process

For each feature area:

1. **Audit** — list every issue under that area from the inventory.
2. **Plan** — for each issue, write the fix sketch in this doc. If schema change is required, flag `🟡` and ASK before proceeding.
3. **Implement** — make the changes; reuse `db_optimizer` helpers (`sql_*`, `_pg_ts`, `sql_true_literal`, etc.) and add new helpers in `core/database_optimization.py` when a pattern recurs.
4. **Smoke test** — `python3 -c "import <module>"` and any existing pytest that covers the file.
5. **PR** — title `pg-audit(<area>): <summary>`, link the inventory rows it closes. Body includes the rows being closed.
6. **Update this doc** — flip status to ✅ and append change-log row.

## Open questions parked for follow-up

- Phase 3: which Postgres-for-tests strategy? Options: (a) `pytest-postgresql` (in-process), (b) `testcontainers-python` (Docker), (c) shared dev Postgres on Render. To decide once Phase 2 lands.
- Phase 4: After all feature areas are PG-clean, do we delete the `if db_type == 'sqlite'` branches in one commit or per file? Decide based on diff size.
- Alembic baseline: do we adopt Alembic properly (Phase 5) or stay with `init_database()` permanently? Decide after schema-change questions surface in Phase 2.
