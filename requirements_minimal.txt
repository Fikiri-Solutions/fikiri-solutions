# Fikiri Solutions - Minimal Requirements
# Lightweight dependencies only

# Core Python libraries (usually included)
# json, base64, pathlib, typing, argparse, sys, os - all built-in

# Google API (minimal)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# AI Services (lightweight - NO heavy ML)
openai>=1.0.0

# Document Processing
beautifulsoup4>=4.0.0
html5lib>=1.1

# Web Framework
flask>=2.0.0
flask-socketio>=5.0.0
flask-cors>=3.0.0
gunicorn>=20.0.0

# Database and ORM
SQLAlchemy>=2.0.0

# Redis for caching, sessions, and queues
redis>=4.0.0
rq>=2.0.0

# Monitoring and error tracking
sentry-sdk[flask]>=2.35.0

# System monitoring
psutil>=5.9.0

# Security and encryption
cryptography>=41.0.0
PyJWT>=2.8.0

# Workflow Automation
fastapi>=0.100.0
uvicorn>=0.20.0
pyyaml>=6.0
click>=8.0
aiohttp>=3.9.0

# Strategic Heavy Dependencies (Optional - Use Feature Flags)
# Uncomment only what you need:
# scikit-learn>=1.0.0                    # For enhanced ML scoring
# Transformers removed for lightweight operation
# PyTorch removed for lightweight operation
# ML dependencies removed for lightweight operation
# opencv-python>=4.0.0                   # For computer vision
# pillow>=8.0.0                          # For image processing

# Optional: For better email parsing
# email-validator>=1.0.0  # Uncomment if needed

# Optional: For logging
# colorlog>=6.0.0  # Uncomment if needed

# Development (optional)
# pytest>=7.0.0  # Uncomment for testing