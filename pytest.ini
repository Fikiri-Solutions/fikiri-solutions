[pytest]
markers =
    contract: real provider contract tests (requires sandbox creds)
    integration: staging integration tests (DB/Redis required)
    concurrency: concurrency/race tests (staging DB engine required)
    load: load/perf tests (not run by default)
